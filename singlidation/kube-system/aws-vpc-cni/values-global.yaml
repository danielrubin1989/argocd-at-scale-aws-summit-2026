# Global defaults for aws-vpc-cni — applied to ALL clusters
aws-vpc-cni:
  replicaCount: 2
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
