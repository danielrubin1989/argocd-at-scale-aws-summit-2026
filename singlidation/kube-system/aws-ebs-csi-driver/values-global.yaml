# Global defaults for aws-ebs-csi-driver — applied to ALL clusters
aws-ebs-csi-driver:
  replicaCount: 2
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
