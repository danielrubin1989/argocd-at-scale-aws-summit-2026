# Global defaults for kyverno-policy-reporter — applied to ALL clusters
policy-reporter:
  replicaCount: 2
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
