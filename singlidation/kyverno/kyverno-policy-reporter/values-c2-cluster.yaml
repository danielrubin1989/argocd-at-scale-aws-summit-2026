# Cluster-specific overrides for c2-cluster
policy-reporter:
  replicaCount: 3
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/kyverno-policy-reporter-c2-cluster
