apiVersion: v2
name: external-dns
version: 1.0.0
description: external-dns deployment managed via singlidation
dependencies:
  - name: external-dns
    version: "1.15.0"
    repository: "https://kubernetes-sigs.github.io/external-dns"
